apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infra-environments
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/Wizlit-Org/msa-logging.git
        revision: main
        directories:
          - path: argocd_apps/in-cluster
            include: infra-*.yaml
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/Wizlit-Org/msa-logging.git
        targetRevision: main
        path: argocd_apps/in-cluster
        directory:
          include: '{{path.basename}}'
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true